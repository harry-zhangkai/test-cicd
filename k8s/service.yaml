apiVersion: v1
kind: Service
metadata:
  name: test-cicd-service
  namespace: namesapce-test-cicd
spec:
  type: LoadBalancer
  selector:
    app: test-cicd
  ports:
    - port: 80
      targetPort: 8080
